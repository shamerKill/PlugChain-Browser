import { FC } from 'react';
import { formatClass } from '../../../tools';

const ComponentsLayoutHeader: FC = () => {
  return (
    <header className={formatClass(['layout-header'])}>
      header
    </header>
  );
};

export default ComponentsLayoutHeader;
